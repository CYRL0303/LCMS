package com.legacypilot.mapper;

/**
 * Placeholder for the future persistence mapper.
 *
 * Current MVP uses in-memory maps inside AnalysisService, so this interface is
 * intentionally empty. When MyBatis or another persistence layer is introduced,
 * project/task/repository/incident queries can be moved here or split into
 * dedicated mapper interfaces.
 */
public interface AnalysisMapper {
}
